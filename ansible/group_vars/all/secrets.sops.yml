# Sample global secrets (encrypt with: just -f ansible/ansible.just sops-encrypt path=ansible/group_vars/all/secrets.sops.yaml)
# Example structure: keys referenced by playbooks/roles
# Replace values and then encrypt with sops
---
# global_example_token: "replace-me"
